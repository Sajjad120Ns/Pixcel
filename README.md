# Excelizing Pictures

This project will convert a picture into an Excel worksheet by coloring cells based on image's pixles color.

## How it works

The script:

- Reads each pixel of the input image
- Extracts its RGB color
- Fills the corresponding Excel cell with that color 

So, each cell acts like a pixel, creating a mosaic-style artwork inside Excel.